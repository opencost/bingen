////////////////////////////////////////////////////////////////////////////////
//
//                             DO NOT MODIFY
//
//                          ┻━┻ ︵ヽ(`Д´)ﾉ︵ ┻━┻
//
//            This source file was automatically generated by bingen.
//
////////////////////////////////////////////////////////////////////////////////

package com.opencost.opencost;

import com.bingen.BinDecoder;
import com.bingen.DecodingContext;
import com.bingen.ReadBuffer;
import java.io.InputStream;
import java.util.LinkedHashMap;
import java.util.Map;

/**
 * Generated binary decoder for {@link ClusterManagement}.
 */
public final class ClusterManagementDecoder implements BinDecoder<ClusterManagement> {

    public static final ClusterManagementDecoder INSTANCE = new ClusterManagementDecoder();

    private static final int VERSION = 16;

    /** 
     * Decodes a {@link ClusterManagement} from a standalone byte array. 
     */
    public static ClusterManagement fromBytes(byte[] data) {
        return INSTANCE.decode(DecodingContext.fromBytes(data));
    }

    /** 
     * Decodes a {@link ClusterManagement} read incrementally from a stream. 
     */
    public static ClusterManagement fromStream(InputStream in) {
        return INSTANCE.decode(DecodingContext.fromStream(in));
    }

    @Override
    public ClusterManagement decode(DecodingContext ctx) {
        ReadBuffer buff = ctx.buffer();
        int version = buff.readUInt8();
        if (version > VERSION) {
            throw new IllegalStateException(
                "Invalid version decoding ClusterManagement: expected " + VERSION + " or less, got " + version);
        }

        ClusterManagement.Builder builder = ClusterManagement.builder();
        Map<String, String> v0;
        if (buff.readUInt8() == 0) {
            v0 = null;
        } else {
            int v1 = buff.readInt(); // map length
            v0 = new LinkedHashMap<>(v1);
            for (int v2 = 0; v2 < v1; v2++) {
                String v3;
                if (ctx.isStringTable()) {
                    v3 = ctx.table().at(buff.readInt()); // string table index
                } else {
                    v3 = buff.readString(); // read string
                }
                String v4;
                if (ctx.isStringTable()) {
                    v4 = ctx.table().at(buff.readInt()); // string table index
                } else {
                    v4 = buff.readString(); // read string
                }
                v0.put(v3, v4);
            }
        }
        builder.labels(v0);
        AssetProperties v5;
        if (buff.readUInt8() == 0) {
            v5 = null;
        } else {
            buff.readInt(); // [compatibility, unused]
            v5 = AssetPropertiesDecoder.INSTANCE.decode(ctx);
        }
        builder.properties(v5);
        Window v6;
        buff.readInt(); // [compatibility, unused]
        v6 = WindowDecoder.INSTANCE.decode(ctx);
        builder.window(v6);
        double v7;
        v7 = buff.readFloat64();
        builder.cost(v7);
        ClusterManagement result = builder.build();
        return result;
    }
}
