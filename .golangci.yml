run:
  timeout: 5m
  skip-dirs:
    - tests

issues:
  # Keep full linter coverage while excluding generated artifacts and test trees.
  exclude-generated: strict
  exclude-files:
    - ".*_codecs\\.go$"
